apiVersion: v1
kind: ConfigMap

metadata:
  name: mealops-config
  labels:
    app.kubernetes.io/name: mealops
    app.kubernetes.io/component: application

data:
  GEMINI_MODEL: "gemini-3.1-flash-lite"
  LOG_LEVEL: "INFO"
  PROVIDER_TIMEOUT_MS: "310000"
