apiVersion: v1
kind: Service

metadata:
  name: mealops-app-service

spec:
  type: LoadBalancer

  selector:
    app: mealops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000