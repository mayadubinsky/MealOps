apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mealops.fullname" . }}-test-connection"
  labels:
    {{- include "mealops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mealops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
