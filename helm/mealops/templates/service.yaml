apiVersion: v1
kind: Service
metadata:
  name: {{ include "mealops.fullname" . }}
  labels:
    {{- include "mealops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mealops.selectorLabels" . | nindent 4 }}
